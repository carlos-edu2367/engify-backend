# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic[email]>=2.12.5
pydantic-settings>=2.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Segurança
python-jose[cryptography]>=3.3.0
passlib[argon2]>=1.7.4
python-multipart>=0.0.9

# Cache
redis[hiredis]>=5.0.0
arq>=0.26.0

# Storage - Supabase Storage (S3-compatible)
boto3>=1.35.0
openpyxl>=3.1.5

# Rate limiting
slowapi>=0.1.9

# HTTP client (também usado pela camada de IA via OpenRouter)
httpx>=0.27.0

# Observabilidade
structlog>=24.0.0

# zoneinfo (app/core/tempo.py) usa o banco IANA do sistema; a imagem de producao
# (Debian) ja o inclui, mas Windows nao tem banco de fuso proprio, entao o dev
# local e o CI nesse SO dependem deste pacote para nao lancar ZoneInfoNotFoundError.
tzdata>=2024.1

# Testes
pytest>=8.0.0
pytest-asyncio>=0.23.0
